name: Integration

on:
  push:
  schedule:
    - cron: '0 0 * * 0' # Weekly on Sundays at 02:00


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v0.4
    - name: Test to html files.
      uses: Cyb3r-Jak3/html5validator-action@master
      with:
        root: src/
